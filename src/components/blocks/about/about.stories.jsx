import React from "react";
import About from "./about";

export default {
	title: 'About (About)'
}

const Template = (args) => <About {...args} />;

export const AboutTemplate = Template.bind({})

AboutTemplate.args = {
}

